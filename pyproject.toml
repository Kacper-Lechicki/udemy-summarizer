[project]
name = "udemy-summarizer"
version = "0.1.0"
description = "Summarize Udemy lectures into structured notes using OpenAI"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=2.30.0",
    "python-dotenv>=1.2.2",
    "requests>=2.33.0",
]
